# Dependencies
node_modules/

# TanStack Start build output
.output/
.vinxi/
dist/
.tanstack/

# Auto-generated by the TanStack Router file-based routing plugin; checked in anyway so that
# `tsc --noEmit` succeeds on a fresh checkout before `vite dev`/`vite build` regenerates it
# for real — see src/routeTree.gen.ts's own header comment.

# src/__generated__/ (relay-compiler output, `npm run relay`) is deliberately NOT ignored —
# Implementation.md m21 requires the Relay artifacts to be committed, the same way
# src/routeTree.gen.ts above is checked in despite being generated.

# Test coverage
coverage/

# Env
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.log
.idea/
.vscode/*
!.vscode/extensions.json
