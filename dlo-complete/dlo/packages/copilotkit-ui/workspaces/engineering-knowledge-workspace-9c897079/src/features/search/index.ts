// src/features/search/index.ts — public surface of the bundle search panel (m21).
export {
  SearchPanel,
  renderHighlightedSnippet,
  DEFAULT_SEARCH_DEBOUNCE_MS,
  DEFAULT_SEARCH_PAGE_SIZE,
  type SearchPanelProps,
  type SearchHitSummary,
  type SearchTimer,
  type SearchTimerHandle,
} from "./SearchPanel";
